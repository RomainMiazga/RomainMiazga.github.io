<nav class="navbar">
  <div class="nav-container">
    <a href="/" class="logo">RM<span>.</span></a>
    <div class="nav-links">
      <a href="/">Accueil</a>
      <a href="/projets">Réalisations & Lab</a>
    </div>
  </div>
</nav>

<style>
  .navbar {
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 100;
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(5, 5, 5, 0.7);
  }
  .nav-container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .logo {
    font-size: 1.5rem;
    font-weight: 800;
    color: #fff;
    text-decoration: none;
  }
  .logo span { color: #00d2ff; }
  .nav-links { display: flex; gap: 2rem; }
  .nav-links a {
    color: #a0a0a0;
    text-decoration: none;
    font-weight: 600;
    transition: 0.3s;
  }
  .nav-links a:hover { color: #fff; }
</style>